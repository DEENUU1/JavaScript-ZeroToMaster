let userHours = prompt("Ile godzin dziennie korzystasz z PC?");
let betterHours = userHours / 2;
alert(`Powinieneś korzystać ${betterHours} godzin`);

  setInterval(() => {
     console.log(innerHeight, innerWidth) 
  }, 1000);
