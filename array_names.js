let names = ["Ania", "Ola", "Kasia", "Olek", "Rafał"];
for (let i = 0; i < names.length; i++) {
    console.log(names[i])
}
for (let i = names.length - 1; i >= 0; i--) {
    let element = names[i];
    console.log(element);

}